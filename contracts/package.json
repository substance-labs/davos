{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Solidity contract for LLM Adapter",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy:local": "forge script script/LLMAdapter.s.sol --broadcast --rpc-url http://localhost:8545 -vvv",
    "lint": "solhint 'src/**/*.sol' 'script/**/*.sol' 'test/**/*.sol'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "solhint": "^4.5.4"
  }
}
